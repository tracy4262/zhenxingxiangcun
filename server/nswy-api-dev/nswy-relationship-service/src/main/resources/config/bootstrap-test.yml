spring:
  application:
    name: nswy-relationship-service
  cloud:
    config:
      enabled: true
      uri: http://${http.config.uri}:8888
      profile: ${http.config.profile}


http:
 config:
   uri: 192.168.7.42
   profile: prod

info:
  app:
    name: "nswy-relationship-service"
    description: "关系圈管理模块"
    version: "1.0-SNAPSHOT"
    spring-boot-version: "1.0-SNAPSHOT"
